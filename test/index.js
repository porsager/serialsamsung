import TV from '../src/index.js'
import { t } from 'fantestic'

/*

t('Turns on TV', { timeout: 50000 }, async() => {
  const tv = TV({ id: 1 })

  return [
    'on',
    await tv.raw(0x00),
    tv.close()
  ]
})

*/
